query RunInput {
  cart {
    lines {
      quantity
    }
    cost {
      subtotalAmount {
        amount
      }
    }
  }
  shop {
    metafield(namespace: "volume-discount", key: "all-offers") {
      value
    }
  }
}
